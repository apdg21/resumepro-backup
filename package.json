{
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf public .cache",
    "clean:dev": "npm run clean && npm run dev",
    "clean:build": "npm run clean && npm run build",
    "preview": "npm run clean:build && npx serve public/",
    
    "deploy": "npm run clean:build && npx gh-pages -d public"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "gh-pages": "^6.0.0"
  }
}
